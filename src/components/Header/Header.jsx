import React from "react";
import Wrapper from "../../styles/Wrapper";

const Header = (props) => {
    return(
        <Wrapper id="header">
            Header
        </Wrapper>
    )
}

export default Header;