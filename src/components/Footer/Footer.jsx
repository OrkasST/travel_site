import React from "react";
import Wrapper from "../../styles/Wrapper";

const Footer = (props) => {
    return(
        <Wrapper >
            Footer
        </Wrapper>
    )
}

export default Footer;